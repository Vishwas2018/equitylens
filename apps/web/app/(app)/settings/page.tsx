export default function SettingsPage() {
  return (
    <div>
      <h1 className="text-[var(--text-2xl)] font-semibold text-[var(--fg-default)]">Settings</h1>
      <p className="mt-[var(--space-2)] text-[var(--text-sm)] text-[var(--fg-muted)]">
        Account and workspace settings — coming Day 12.
      </p>
    </div>
  );
}
