Checkpoint: D01-T6 — Vercel deployment configuration
Date: 2026-05-19
Branch: feature/d01-bootstrap

--- Files created ---
vercel.json
  framework: nextjs
  rootDirectory: apps/web
  buildCommand: cd ../.. && pnpm build --filter=@equitylens/web (monorepo-aware)
  installCommand: pnpm install --frozen-lockfile
  Security headers on all routes: X-Content-Type-Options, X-Frame-Options,
    X-XSS-Protection, Referrer-Policy
  deploymentEnabled: main only (feature branches deploy on PR via Vercel GitHub integration)

.vercelignore
  Excludes: node_modules, .turbo, *.tsbuildinfo, docs/process/prompts,
            scripts, .husky, .github, __tests__, *.test.ts, *.spec.ts

--- Step awaiting human ---
Run: pnpm dlx vercel link
  Select: existing project OR create "equitylens"
  Framework: Next.js (auto-detected)
  Root directory: apps/web

Expected outputs to capture:
  - Preview URL (https://equitylens-xxx.vercel.app)
  - curl https://<preview-url>/api/health → {"ok":true,"version":"<sha>"}

--- Verification (post-link) ---
[ ] .vercel/project.json exists
[ ] pnpm dlx vercel --prod deploys successfully (or Vercel GitHub integration auto-deploys)
[ ] curl /api/health returns {"ok":true,"version":"<sha>"}

--- Fix applied (housekeeping commit under D01-T6) ---
Vercel schema validation failed: "should NOT have additional property 'rootDirectory'".
Also removed: outputDirectory (Next.js auto-detects), git.deploymentEnabled (dashboard).
Added: regions: ["syd1"] (Sydney — Australian deployment target).
Kept: buildCommand, installCommand, framework, headers.
Commit: fix(vercel): remove unsupported rootDirectory from vercel.json [D01-T6]
DEV-0009 (to log at D01-T7): rootDirectory based on common monorepo pattern;
  not supported in Vercel's schema. Spec corrected. Severity: low, accepted.

Vercel dashboard action required:
  Settings → General → Node.js Version → set to "20.x" (match .nvmrc)

--- Final verification (2026-05-19) ---
Preview URL: https://equitylens-26137hhyi-vishwas2018s-projects.vercel.app
  GET /    → "EquityLens — bootstrap" (HTTP 200, static page ✅)
  GET /api/health → {"ok":true,"version":"dev"} (HTTP 200 ✅)

Note: version="dev" is correct — BUILD_SHA env var not set in Vercel project.
GitHub Actions CI sets BUILD_SHA=${{ github.sha }}; direct Vercel builds use fallback.

Final vercel.json: framework, buildCommand (cd ../.. monorepo), installCommand (cd ../..),
  outputDirectory: ".next" (relative to Root Directory apps/web), regions: ["syd1"], headers.
Dashboard: Root Directory = apps/web, Node.js = 20.x, Output Directory = (cleared).

[x] .vercel/ (link config managed by Vercel dashboard integration)
[x] Deployment live and serving from syd1 region
[x] /api/health returns {"ok":true,"version":"dev"}

=== OVERALL: PASS ===
